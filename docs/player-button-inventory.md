# Player Button And Control Inventory

Captured after the player handoff tasks through `d890c84`. Line numbers may drift as Phase G changes land.

Decision vocabulary: `keep`, `relabel`, `disable-when-X`, `merge-with-Y`, `move-to-Z`, `remove`.

| Control | Location (file:line) | Current behavior | Expected behavior | Decision | Tooltip / aria-label |
| --- | --- | --- | --- | --- | --- |
| Desktop favorite | `frontend/src/lib/components/now-playing/NowPlayingTransport.svelte:62` | Toggles the current library track favorite state; disabled without a track or while pending. | Keep favorite available only when the active item can be represented as a library track. | `keep` | `aria-label` switches between add/remove favorite; title mirrors it. |
| Desktop shuffle | `frontend/src/lib/components/now-playing/NowPlayingTransport.svelte:72` | Cycles shuffle modes: off, genre, weighted, true shuffle. | Keep the full mode cycle, but make state and shortcut discoverability clearer in the help overlay. | `keep` | State-aware title and `aria-label` already present. |
| Desktop previous | `frontend/src/lib/components/now-playing/NowPlayingTransport.svelte:81` | Skips to previous track. | Keep as transport control; disabled while no playback action is possible. | `keep` | `aria-label="Previous track"`; title present. |
| Desktop play/pause | `frontend/src/lib/components/now-playing/NowPlayingTransport.svelte:82` | Toggles playback. | Keep as the primary transport action. | `keep` | State-aware `aria-label` and title present. |
| Desktop next | `frontend/src/lib/components/now-playing/NowPlayingTransport.svelte:85` | Skips to next track. | Keep as transport control; should remain safe with pending queue entries. | `keep` | `aria-label="Next track"`; title present. |
| Desktop repeat | `frontend/src/lib/components/now-playing/NowPlayingTransport.svelte:86` | Cycles repeat off/all/one. | Keep the cycle and surface the current mode consistently in shortcuts/help. | `keep` | State-aware title and `aria-label` already present. |
| Desktop more actions | `frontend/src/lib/components/now-playing/NowPlayingTransport.svelte:95` | Opens the shared track context menu for the current track. | Keep as the canonical now-playing overflow entry point. | `keep` | `aria-label="More actions"`; title present. |
| Quiet mode launcher | `frontend/src/routes/+layout.svelte:997` | Opens Quiet Mode. | Keep in the now-playing surface. | `keep` | `aria-label="Quiet Mode"`; title present. |
| Mute and volume | `frontend/src/routes/+layout.svelte:1038` / `frontend/src/routes/+layout.svelte:1059` | Mutes/unmutes and changes volume. | Keep in desktop player controls; no queue-specific changes needed. | `keep` | Mute has title/`aria-label`; slider has `aria-label="Volume"`. |
| Queue banner | `frontend/src/routes/+layout.svelte:1091` | Expands or collapses the queue by clicking the queue count banner. | Keep as the large queue affordance. | `keep` | State-aware title, `aria-label`, and `aria-expanded` present. |
| Automix queue toggle | `frontend/src/routes/+layout.svelte:1110` | Toggles automix. | Keep in queue header; make shortcut/help wording match the transport toggle. | `keep` | State-aware title and `aria-label` present. |
| Discover New toggle | `frontend/src/routes/+layout.svelte:1117` | Toggles external discovery for automix. | Keep beside automix, disabled unless automix is enabled. | `disable-when-automix-off` | State-aware title and `aria-label` present. |
| Save queue | `frontend/src/routes/+layout.svelte:1129` | Opens the save-queue form. | Keep in queue header. | `keep` | `aria-label="Save queue as playlist"`; title present. |
| Clear queue | `frontend/src/routes/+layout.svelte:1137` | Clears upcoming queue and supports a short undo window by keyboard. | Keep in queue header as the main destructive queue action. | `keep` | `aria-label="Clear queue"`; title present. |
| Queue expand icon | `frontend/src/routes/+layout.svelte:1145` | Secondary expand/collapse button. | Keep for icon-first users; behavior should match queue banner. | `keep` | State-aware title, `aria-label`, and `aria-expanded` present. |
| Save queue confirm/cancel | `frontend/src/routes/+layout.svelte:1171` / `frontend/src/routes/+layout.svelte:1175` | Saves or cancels playlist creation. | Keep inline with the save form. | `keep` | Confirm text is visible; cancel has `aria-label="Cancel save queue"`. |
| Pending queue row | `frontend/src/routes/+layout.svelte:1194` | Row is visibly resolving, `aria-disabled`, and removed from tab flow. | Keep rows non-interactive while resolution is pending. | `disable-when-pending` | Pending text and resolving titles present. |
| Queue reason button | `frontend/src/routes/+layout.svelte:1247` | Shows or toggles the "why is this here" reason for generated queue items. | Keep for explainability; hide when no useful reason exists. | `keep` | Title includes reason text. |
| Queue row favorite | `frontend/src/routes/+layout.svelte:1259` | Toggles favorite for normal rows; disabled for pending rows. | Keep, disabled whenever the row is unresolved. | `disable-when-pending` | State-aware title and `aria-label` present. |
| Queue row more | `frontend/src/routes/+layout.svelte:1267` | Opens library or TIDAL context menu for the row. | Keep as row overflow; use TIDAL menu for playable ephemeral rows. | `keep` | `aria-label="More actions"`; title present. |
| Queue row play next | `frontend/src/routes/+layout.svelte:1273` | Moves persisted rows next; promotes ephemeral TIDAL mix rows into the queue. | Keep; wording should distinguish move versus promote. | `relabel` | Title changes for ephemeral TIDAL rows; `aria-label` is generic. |
| Queue row remove | `frontend/src/routes/+layout.svelte:1279` | Removes persisted rows; disabled for ephemeral TIDAL mix rows. | Keep disabled for ephemeral rows until backend removal exists. | `disable-when-ephemeral` | Title explains disabled TIDAL mix rows; `aria-label` remains remove. |
| Mobile mini-player open | `frontend/src/routes/+layout.svelte:1310` | Opens the mobile now-playing sheet. | Keep as the mobile entry point. | `keep` | `aria-label="Open now playing"`. |
| Mobile mini-player play/pause/next | `frontend/src/routes/+layout.svelte:1327` / `frontend/src/routes/+layout.svelte:1335` | Toggles playback and skips next from the collapsed player. | Keep as compact transport controls. | `keep` | State-aware play label; next has `aria-label="Next"`. |
| Mobile more navigation | `frontend/src/routes/+layout.svelte:1384` / `frontend/src/routes/+layout.svelte:1399` | Opens and closes extra navigation links. | Keep outside player UX pass. | `keep` | `aria-label="More navigation"` and `aria-label="Close more navigation"`. |
| Mobile now-playing controls | `frontend/src/routes/+layout.svelte:1467` / `frontend/src/routes/+layout.svelte:1492` / `frontend/src/routes/+layout.svelte:1502` | Favorite, seek, previous, play/pause, next, shuffle, repeat, and automix in the mobile sheet. | Keep and align labels with desktop transport during shortcut/help polish. | `keep` | Mostly state-aware `aria-label`s; seek slider has `aria-label="Seek"`. |
| Mobile queue row actions | `frontend/src/routes/+layout.svelte:1599` / `frontend/src/routes/+layout.svelte:1604` / `frontend/src/routes/+layout.svelte:1610` | More, play next/promote, remove in mobile queue rows. | Keep behavior aligned with desktop queue rows. | `keep` | Titles mirror desktop; remove disabled for ephemeral rows. |
| Library track row | `frontend/src/lib/components/TrackRow.svelte:104` | Row click plays/selects depending on variant; right-click opens library menu. | Keep row click and context menu behavior. | `keep` | Row has keyboard support; action buttons carry labels. |
| Library track row actions | `frontend/src/lib/components/TrackRow.svelte:127` / `frontend/src/lib/components/TrackRow.svelte:136` / `frontend/src/lib/components/TrackRow.svelte:145` | Add to queue, song radio, more menu, and favorite in supported row variants. | Keep library actions as the model for local tracks. | `keep` | Titles and `aria-label`s present. |
| TIDAL track row play/add | `frontend/src/lib/components/TidalTrackRow.svelte:125` / `frontend/src/lib/components/TidalTrackRow.svelte:132` | Play and add-to-queue are disabled unless `canPlayTrack` passes. | Keep as the expected gate for TIDAL rows. | `disable-when-not-playable` | Disabled title explains unavailable preview/playback. |
| TIDAL track row radio/more | `frontend/src/lib/components/TidalTrackRow.svelte:139` / `frontend/src/lib/components/TidalTrackRow.svelte:145` | Starts TIDAL radio or opens TIDAL menu. | Keep; menu items should remain gated by playability. | `keep` | Titles and `aria-label`s present. |
| Search hint chips | `frontend/src/routes/search/+page.svelte:619` | Applies suggested searches. | Keep outside player pass. | `keep` | Visible text; no extra label needed. |
| Search filter chips and mode pills | `frontend/src/routes/search/+page.svelte:643` / `frontend/src/routes/search/+page.svelte:661` | Removes filters and changes local/TIDAL/both search mode. | Keep outside player pass. | `keep` | Filter removal has title and `aria-label`; mode buttons are visible text. |
| Search library row play/more | `frontend/src/routes/search/+page.svelte:718` / `frontend/src/routes/search/+page.svelte:737` / `frontend/src/routes/search/+page.svelte:743` | Plays local results and opens library menu. | Keep; should match `TrackRow` semantics. | `keep` | Titles and `aria-label`s present. |
| Search top result play | `frontend/src/routes/search/+page.svelte:808` | Plays the top result using type-specific behavior. | Keep, but only show/enable when the top result is actually playable. | `disable-when-not-playable` | `aria-label="Play top result"`; title present. |
| Search TIDAL album/playlist play | `frontend/src/routes/search/+page.svelte:878` / `frontend/src/routes/search/+page.svelte:934` | Starts TIDAL album or playlist playback. | Keep; ensure empty/unplayable containers do not expose dead play controls. | `disable-when-not-playable` | Titles and `aria-label`s present. |
| Search TIDAL track actions | `frontend/src/routes/search/+page.svelte:1010` / `frontend/src/routes/search/+page.svelte:1017` / `frontend/src/routes/search/+page.svelte:1024` / `frontend/src/routes/search/+page.svelte:1030` | Play/add are gated by playability; radio and more remain available. | Keep the gating and shared TIDAL menu behavior. | `disable-when-not-playable` | Disabled title explains unavailable preview/playback. |
| Discover side-panel actions | `frontend/src/lib/components/DiscoverSpace/DiscoverSidePanel.svelte:271` | Play now, play next, radio, lock anchor, playlist toggle, and hide-from-radio. | Keep; pending Last.fm tracks should keep resolving spinners on playback actions. | `keep` | Titles and `aria-label`s present; pending labels switch to resolving text. |
| Album detail controls | `frontend/src/lib/components/AlbumDetailPopup.svelte:39` / `frontend/src/lib/components/AlbumDetailPopup.svelte:58` / `frontend/src/lib/components/AlbumDetailPopup.svelte:88` | Close, play all, shuffle, row click/context menu, and row overflow. | Keep; row menu should use the same library menu decisions. | `keep` | Main buttons have visible text or `aria-label`; row menu has title. |
| Quiet Mode controls | `frontend/src/lib/components/QuietMode.svelte:128` / `frontend/src/lib/components/QuietMode.svelte:181` / `frontend/src/lib/components/QuietMode.svelte:190` | Exit quiet mode, run searches, and open search from empty state. | Keep; not part of queue cleanup except now-playing menu consistency. | `keep` | Exit/open search have labels; search pills use visible text. |
| Trending mural play/context | `frontend/src/lib/components/charts/TrendingShelf.svelte:284` / `frontend/src/lib/components/charts/ChartMural.svelte:86` | Mural play action plays local or TIDAL tracks; right-click opens matching track menu for the focused or selected entry. | Keep, gated by current entry playability. | `disable-when-not-playable` | Primary action has visible text; mural tiles have labels and titles. |
| Shared library context menu | `frontend/src/lib/player/track_menu.ts:57` | Supplies play now, play next, add to queue, song radio, playlist/favorite options, remove when queue row, and clear all upcoming. | Keep common library actions, but remove global queue clearing from per-track menus. | `move-to-queue-header` | Menu labels are visible text. |
| Shared TIDAL context menu | `frontend/src/lib/player/track_menu.ts:166` | Supplies TIDAL play now, play next, add to queue, song radio, and disabled labels for unplayable rows. | Keep as canonical TIDAL menu; ensure callers pass only normalized `TidalPlayable` data. | `keep` | Disabled menu labels explain unavailable playback. |
| Keyboard shortcut help | `frontend/src/routes/+layout.svelte:265` | Global key handler exists, but there is no visible shortcut/help control. | Add a small help affordance and overlay before deeper control relabeling. | `move-to-queue-header` | Needs new tooltip/`aria-label` in Task 22. |
